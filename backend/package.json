{
  "name": "stock-prompt",
  "version": "1.0.0",
  "description": "Full stack stock prompt web app written in React and Node",
  "scripts": {
    "build": "tsc",
    "start": "nodemon --watch src --exec ts-node src/app.ts",
    "dev": "npm run build && npm start",
    "test": "test"
  },
  "dependencies": {
    "axios": "^1.3.4",
    "express": "^4.18.2",
    "memcached": "^2.2.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
